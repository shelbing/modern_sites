# Font Configuration for POS Hotel Property
# 
# This file contains font selection configuration specific to the POS property.
# Property-specific YAML allows each property to have its own typography identity.

# Selected font combination (1-23)
selectedFontCombination: 13

# Debug mode
debugFonts: false
